import { ScrollView, Text } from "react-native";

export default function PrivacyPolicy(){
    return(
        <ScrollView>
            <Text>
                Privacy Policy
            </Text>
        </ScrollView>
    )
}