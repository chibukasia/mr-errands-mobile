import { ScrollView, Text } from "react-native";

export default function Home(){
    return(
        <ScrollView>
            <Text style={{color: '#fff'}}>
                Here is home
            </Text>
        </ScrollView>
    )
}